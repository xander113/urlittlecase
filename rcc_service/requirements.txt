# YourLittleCase! RCCService dependencies
# Install via start.sh (handles the two-phase pyrender/PyOpenGL install).
#
# DO NOT add pyopengl here — pyrender pins PyOpenGL==3.1.0 exactly.
# start.sh installs pyrender with --no-deps then adds PyOpenGL==3.1.0 manually.

flask>=3.0.0,<4.0
Pillow>=10.0.0
numpy>=1.24.0,<2.0.0
trimesh>=4.0.0
sqlalchemy>=2.0.0
pymysql>=1.1.0
psycopg2-binary>=2.9.0
gunicorn>=21.0.0
